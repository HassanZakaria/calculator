import React from "react";
import { Button } from "react-bootstrap";

const Multiply = () => {
  return (
    <Button variant="warning" className="calc-btn multiply">
      &times;
    </Button>
  );
};
export default Multiply;
