import React from "react";
import { Button } from "react-bootstrap";

const Clear = () => {
  return (
    <Button variant="danger" className="calc-btn clear">
      AC
    </Button>
  );
};
export default Clear;
