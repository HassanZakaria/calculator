import React from "react";
import { Button } from "react-bootstrap";

const Divide = () => {
  return (
    <Button variant="warning" className="calc-btn divide">
      &divide;
    </Button>
  );
};
export default Divide;
