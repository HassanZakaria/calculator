import React from "react";
import Button from "react-bootstrap/Button";

const Equal = () => {
  return (
    <Button variant="warning" className="calc-btn equal">
      =
    </Button>
  );
};
export default Equal;
